\beginsection Binomial distribution

Random variable $X$ is the number of successes in $n$ trials.
$$p(x)={}_nC_xp^x(1-p)^{n-x}$$
$$\hbox{\tt BINOMDIST(number\_s,trials,probability\_s,cumulative)}$$
$$\mu=np$$
$$\sigma^2=np(1-p)$$
$$F(t)=\sum_{x=0}^{\lfloor t\rfloor}p(x),\qquad \hbox{use cumulative=true}$$
$$M(t)=[e^tp+(1-p)]^n$$

\bigskip
\noindent{\it 141.tex}

\end
