\parindent=0pt
\nopagenumbers

18.5. (b) Show that Example 1 can be viewed as a special case of part (a).

\bigskip
Example 1 uses the ``little trick'' of defining $g(x)=f(x)-x$.
This is a special case because $x$ is a specific instance of a general
function.

\vfill
\end
